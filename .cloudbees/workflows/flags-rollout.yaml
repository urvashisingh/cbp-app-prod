apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: flags-rollout
on:
  workflow_dispatch:
    inputs:
      flags:
        type: string
        required: true
      environment:
        type: string
        required: true
jobs:
  rollout-flags:
    steps:
      - uses: docker://golang:1.20.3-alpine3.17
        shell: sh
        run: |
          echo "Step 1 to rollout flags ${{ inputs.flags }} for enviroment ${{ inputs.environment }}"
      - uses: docker://golang:1.20.3-alpine3.17
        shell: sh
        run: |
          echo "Step 2 to rollout flags ${{ inputs.flags }} for enviroment ${{ inputs.environment }}"    
  