apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: deployer

on:
  workflow_call:
    inputs:
      environment:
        type: string
        required: true
      manifest:
        type: string
        required: true
  workflow_dispatch:
    inputs:
      environment:
        type: string
        required: true
      manifest:
        type: string
        required: true
jobs:
  #comp1-art-sample-go-app:
  #  if: ${{ fromJSON(inputs.manifest)['comp1']['index.docker.io/cdrosamples/my-sample-go-app'].deploy }}
  #  uses: urvashisingh/comp1/.cloudbees/workflows/deploy.yaml
  #  with:
  #    artifact-id: ${{ fromJSON(inputs.manifest)['comp1']['index.docker.io/cdrosamples/my-sample-go-app'].id }}
  #    environment: ${{ inputs.environment }}
  
  comp2-art2-fe:
    if: ${{ fromJSON(inputs.manifest)['comp2']['art2-fe'].deploy }}
    uses: urvashisingh/comp2/.cloudbees/workflows/deploy.yaml
    with:
      artifact-id: ${{ fromJSON(inputs.manifest)['comp2']['art2-fe'].id }}
      environment: ${{ inputs.environment }}
  comp2-art2-be:
    if: ${{ fromJSON(inputs.manifest)['comp2']['art2-be'].deploy }}
    uses: urvashisingh/comp2/.cloudbees/workflows/deploy.yaml
    with:
      artifact-id: ${{ fromJSON(inputs.manifest)['comp2']['art2-be'].id }}
      environment: ${{ inputs.environment }}
  comp1-art1:
    if: ${{ fromJSON(inputs.manifest)['comp1']['art1'].deploy }}
    uses: JCWook/sample-go-app/.cloudbees/workflows/deploy.yaml
    with:
      artifact-id: ${{ fromJSON(inputs.manifest)['comp1']['art1'].id }}
      environment: ${{ inputs.environment }}

